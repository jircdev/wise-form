{
    "name": "wise-form-workspace",
    "version": "1.0.0",
    "private": true,
    "description": "Wise Form workspace",
    "scripts": {
        "dev": "pnpm --filter @bgroup/wise-form dev",
        "build": "pnpm --filter @bgroup/wise-form build",
        "watch": "pnpm --filter @bgroup/wise-form watch",
        "clean": "pnpm --filter @bgroup/wise-form clean"
    },
    "devDependencies": {
        "pnpm": "^8.0.0"
    }
}
